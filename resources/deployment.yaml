apiVersion: batch/v1
kind: Job
metadata:
  name: test-api
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        app: test-api
    spec:
      restartPolicy: Never
      containers:
        - image: olehzahnitko/argocd:1.3.5
          name: test-api
          ports:
            - containerPort: 1700
