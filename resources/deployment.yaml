apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapi
spec:
  replicas: 2
  revisionHistoryLimit: 4
  selector:
    matchLabels:
      app: testapi
  template:
    metadata:
      labels:
        app: testapi
    spec:
      containers:
        - image: olehzahnitko/argocd:1.1.0
          name: testapi
          ports:
            - containerPort: 1700
